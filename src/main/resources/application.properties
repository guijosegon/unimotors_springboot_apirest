spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:unimotors}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postallan}
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# JWT (configure a secret before running in production)

app.jwt.segredo=${APP_JWT_SEGREDO:2a54e3e2d5b1c7a9f283d8f4a6b7c9d1245e78a9b3c4d5e6f7a8b9c0d1e2f3a}


# app.jwt.segredo=${APP_JWT_SEGREDO:change-me-please} alterado chave pois não estava iniciando
app.jwt.expMinutos=60

# Server
server.port=${PORT:8080}

# Logging
logging.level.org.springframework=INFO
